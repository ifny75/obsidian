map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

# Что попадает в журнал доступа.
#
# Формат по умолчанию пишет User-Agent, а он у мессенджера — не «браузер»,
# а точная модель телефона и сборка ОС: строки вида
# "Dalvik/2.1.0 (Linux; U; Android 17; Pixel 8 Pro Build/CP2A.260805.005)"
# вместе со временем каждого запуска приложения лежали здесь две недели.
# Ни адреса, ни User-Agent, ни referer тут больше нет: для дела хватает
# времени, кода ответа и пути, а всё остальное — досье на человека, которое
# мы завели бы себе сами.
log_format obsidian '$time_iso8601 $status $request_method $uri $body_bytes_sent';

# Вход определяется портом, на котором принят запрос, и ничем больше:
# 8082 — Tor, остальное — Cloudflare. Подробнее в obsidian-proxy.conf.
map $server_port $obsidian_route {
    8082    onion;
    default cloudflare;
}

# На onion-входе адреса клиента не существует: заголовок обнуляется, и
# приложение считает такие соединения одним общим ведром с отдельными лимитами.
# На остальных входах приходит то, что поставил Cloudflare, либо — на втором
# этапе Multi-hop — то, что передал первый узел, куда чужому не достучаться.
map $server_port $obsidian_client_ip {
    8082    "";
    default $http_cf_connecting_ip;
}

# Публичная точка входа cloudflared и Tor onion service.
server {
    listen 127.0.0.1:8080;
    listen 127.0.0.1:8082;
    server_name _;
    access_log /var/log/nginx/access.log obsidian;
    client_max_body_size 17m;

    location = /multihop/ws {
        # Длительность и объём сессии — тоже сведения о человеке.
        access_log off;
        proxy_pass http://10.77.0.3:8081/ws;
        include /etc/nginx/snippets/obsidian-proxy.conf;
    }
    location ^~ /multihop/v1/ {
        proxy_pass http://10.77.0.3:8081/v1/;
        include /etc/nginx/snippets/obsidian-proxy.conf;
    }
    location = /ws {
        # Прямой маршрут до main: сессию сокета не журналируем по той же
        # причине, что и второй этап Multi-hop.
        access_log off;
        proxy_pass http://10.77.0.1:8080;
        include /etc/nginx/snippets/obsidian-proxy.conf;
    }
    location / {
        proxy_pass http://10.77.0.1:8080;
        include /etc/nginx/snippets/obsidian-proxy.conf;
    }
}

# Принимает только второй этап Multi-hop от другого relay по WireGuard.
server {
    listen 10.77.0.2:8081;
    server_name _;
    # Второй этап Multi-hop — это чужая сессия целиком: путь, длительность,
    # объём. Журналировать её здесь незачем ровно по тем же причинам, что и
    # на входе.
    access_log off;
    client_max_body_size 17m;
    allow 10.77.0.3;
    deny all;

    location / {
        proxy_pass http://10.77.0.1:8080;
        include /etc/nginx/snippets/obsidian-proxy.conf;
    }
}
