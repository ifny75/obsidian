# Свой блок короткий: перед ним Cloudflare подставляет управляемый, где уже есть
# группа User-agent: * — вторая такая же только путала бы читающего.
#
# Страницы /channel/<имя> закрыты не здесь, а мета-тегом noindex на самой
# странице: Disallow запретил бы её читать, и тег никто бы не увидел.

Sitemap: https://valanium.com/sitemap.xml
